{
  "name": "@buildapp/analysis-service",
  "version": "0.1.0",
  "private": true,
  "description": "The one link-analysis pipeline, as a reusable application service: URL or sealed source package in, candidate + model + mobile scene bundle + hashes out, with real stage progress and cancellation. The CLI and the analyzer HTTP API are both thin adapters over it. Knows no project.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@buildapp/geometry": "*",
    "@buildapp/mobile-scene": "*",
    "@buildapp/model": "*",
    "@buildapp/reconstruction": "*",
    "@buildapp/source-analyzer": "*",
    "@buildapp/source-common": "*",
    "@buildapp/source-metrics": "*",
    "@buildapp/source-observations": "*",
    "@buildapp/source-package": "*",
    "@buildapp/source-vision": "*"
  },
  "devDependencies": {
    "@buildapp/synthetic-drawings": "*"
  }
}
