{
  "name": "@buildapp/mobile-scene",
  "version": "0.1.0",
  "private": true,
  "description": "Derived mobile scene bundle: CanonicalBuildingModel + CompiledScene -> a deterministic JSON asset the native mobile viewer loads. Derived data only; never a second source of truth. No React, no Three.js, no renderer.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": { "@buildapp/model": "*", "@buildapp/geometry": "*" },
  "devDependencies": { "@buildapp/commands": "*", "@buildapp/demo": "*", "@buildapp/reference-marcowki": "*" }
}
