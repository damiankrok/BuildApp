# STAGE BUILDAPP-03Y — Exterior Closure and Semantic Styling

Branch `claude/buildapp-buildworld-v1-7y6yqh`. The candidate under review is
**Marcówki (auto v3)** (`marcowki-auto-v3`), sealed beside the Auto v2 baseline
the owner reviewed. Every number below is regenerated by a command named next
to it.

## START HEAD

`31dced4a42a92ad18f446d8dfe870e0c59b95a98` — the final HEAD of BUILDAPP-03X
(CI run 36057369183 green on `9f67eb9`).

## FINAL HEAD

The commit that carries this report and its CI record (see `git log`); it
changes documents only. Stage commits: `1a9f514` (schema 1.5.0, closure
audit, assembly closure, tones, styling, Auto v3), `ba71193` (gaps, stacked
returns, terraces to returns, finish runs, cladding, the exterior evaluation
and its CI step), `1437954` (secondary-finish styling, per-return tones, the
§17 source-view audit, subtle AO on Android, docs, screenshots), `74d4646`
(Android JVM tests hold Auto v3 to every per-candidate check), `603eb2e`
(the CI signer check reads the build's own apksigner) — the head CI proved
green (run 36240702093).

## BASELINE OWNER REVIEW

The owner reviewed Auto v2 on Android: the composition better than Auto v1,
the exterior unfinished. Observed: joints not clean (penetrations; walls,
slabs, roof and trims intersecting); the balcony wrong (its short side does
not end at the building; the railing should turn and return); the terrace
weak; facade bands and frames incomplete; a jagged roof edge, verge and
fascia; elements merging visually. Wanted: a clean, minimal, stylised
architectural graphic, not photoreal. These observations were used as a
benchmark signal (§18): they named what to look at; nothing below is tuned
to them, and every rule is proved on generic fixtures.

## EXTERIOR FORENSIC AUDIT

`docs/MARCOWKI_AUTO_V2_EXTERIOR_FORENSIC_AUDIT.md`: eight defects, each with
OWNER_OBSERVATION, SOURCE_SUPPORT, CURRENT_MODEL_OBJECTS, FIRST_BAD_PIPELINE_STAGE,
ROOT_CAUSE, FIX_LAYER and QUALITY_AFTER_FIX, measured by the closure audit on
the sealed Auto v2 (`stage-reports/artifacts/exterior-closure/closure-marcowki-auto-v2.json`).

## ROOT CAUSES

| Defect | First bad stage | Root cause | Fix layer |
|---|---|---|---|
| slab / flat-roof edges in the facade planes | DSL emission | a plate could not be said to bear into its walls | DSL command + compiler (`plateInset`, slabs inset by T) |
| verge bars through the roof and returns | model | no roof-edge semantics: a verge was a free bar over a plate | schema (`Roof.edgeMembers.verge`) + compiler + topology solver |
| portal head through the garage roof | model + emission | the flat roof's edge could not be its edge | schema (`edgeMembers.fascia`) + topology solver |
| balcony ends short / into returns / 3 cm low | topology solver | slab = readable band; no end conditions | topology solver (`closeBalconies`) + metric solver (floor datum) |
| railing free end 0.94 m | model + topology solver | railings could not turn; nothing to run them to | schema (`Railing.path`) + compiler + topology solver |
| terrace weak | model + analyzer | no terrace object; no platform reader | schema (`terraces`) + DSL + compiler + analyzer |
| band broken, frames stepped | topology solver | members solved one by one, no assembly graph | topology solver (assembly closure, facade graph) |
| elements merge | styling system + analyzer | colour by part; tones read against an absolute threshold | semantic groups, palette, tone chain, viewers |

No compiler defect was fixed with an analyzer heuristic, no reconstruction
error with a renderer trick, and no tolerance was widened.

## GEOMETRY CLOSURE

`packages/geometry/src/closure.ts` — `geometryClosureAudit(model, scene)`.
For every pair of structural solids: shared volume (ray-parity sampling),
same-facing coplanar area (polygon clipping; area under a third solid is
enclosed and not counted), and, for pairs the model says meet within 10 cm,
the gap (vertex-to-triangle distance). The intended relation comes from the
model — CONTACT, BEARING (a plate may share at most wall T × plate thickness ×
run), FLUSH, PENETRATION (a chimney through its roof), GUARDS, SEPARATION —
over the §4 labels: WALL↔SLAB, WALL↔ROOF, WALL↔RETURN_WALL,
MAIN_MASS↔ATTACHED_MASS, BALCONY_SLAB↔WALL, BALCONY_SLAB↔FASCIA,
RAILING↔BALCONY, PARAPET↔FLAT_ROOF, FACADE_FRAME↔HOST_FACADE,
TERRACE↔EXTERIOR_FLOOR_DATUM, TRIM↔ROOF. Railing ends are measured to walls
on their real inward side or to another railing; terraces against their
floor datum; slivers, non-manifold edges and disconnected members per solid.
Findings carry scope EXTERIOR or INTERIOR. Nothing is repaired.

Tests: `packages/geometry/test/closure.test.ts` (15).

## WALL / SLAB JOINTS

Upper slabs are emitted inset by the full wall thickness, holes clipped to the
slab; roofs bear on their walls through `plateInset`. §5 tests: a ring of
walls is clean; a T joint meets the inner face (a partition run into a wall
with no junction is refused by the DSL, `WALLS_OVERLAP`; one stopping 3 cm
short is a 0.03 m GAP); a butt joint meets and does not enter; a slab bearing
to the walls' centrelines is within its allowance with no facade-plane face;
a slab run to the outer face is flagged; a balcony against its wall is clean
and pushed 10 cm in is an INTERSECTION on BALCONY_SLAB↔WALL; a facade member
on / 5 cm off / pushed into its host is clean / a 0.05 m GAP / an
INTERSECTION.

## ROOF EDGE / VERGE / FASCIA

Schema 1.5.0 `Roof.edgeMembers`: `verge` boards per gable end (width
vertical, depth along the ridge axis) and `fascia` boards on named edges,
compiled with the roof: the plate shortened by the strips, one chevron per
gable, walls under a strip following the board's underside. Auto v3: verge
0.79 / 0.79 m vertical, 0.61 m across the rake (truth 0.83 / 0.64), 0.99 m
and 1.00 m deep — the depth of the zone the returns stand in
(`closeVerges`); the garage roof's front fascia is the portal head. 0 trim
findings (Auto v2: 22).

## GARAGE JOIN

The garage stands against the main body along x 7.90; its flat plate bears
0.245 m into its walls on every side and the parapet rises past it; the
portal head is its fascia. 0 findings (Auto v2: 4 plate edges in the garage
walls' planes). Fixture 4 (attached garage with parapet) holds it; the same
garage with no plate inset is flagged PARAPET↔FLAT_ROOF.

## BALCONY

`BalconyAssembly` semantics in `closeBalconies` (`assembly-closure.ts`): each
end is WALL (a lower-storey return beside it), CARRIES (a same-storey return
standing on it over an open recess below, and only where the elevation draws
the fascia running on under it — `bandContinues`), FREE (the plan read for
the balustrade line across the zone) or MEETS (the next band member). The
top snaps to the storey floor within 0.1 m. Stacked returns first share one
face (`alignStackedReturns`).

| | Auto v2 | Auto v3 | truth |
|---|---|---|---|
| front slab x | 3.135..7.230 | 3.310..7.900 | 3.25..7.90 |
| front ends | — | FREE (plan line at 3.31) / CARRIES `return-front-1-1`, MEETS the portal head | |
| rear slab x | 0.639..7.176 | 0.645..7.255 | 0.61..7.29 |
| rear ends | — | WALL / WALL | |
| tops | 3.029 / 3.026 | 3.060 / 3.060 | 3.06 |

Kelsall (returns on a closed storey, band stopping at their faces) now stops
at the return faces instead of running under them — the CARRIES rule needs
the drawn band.

## RAILING RETURNS

Schema 1.5.0 `Railing.path`; the compiler draws a polyline railing with one
square post per corner, end posts inside the run and rails stopping at the
corner posts. `railingPath` runs along the mouth, turns back to the wall at a
FREE end where the plan draws the turn, and stops at WALL ends. Auto v3 front
railing `(3.36, 0.97) → (3.36, 0.05) → (7.22, 0.05)`: 2 runs, 1 turn; rear:
1 run between the returns. 0 free ends; largest end distance 0.025 m (half a
post). Auto v2: 2 free ends, the front one 0.94 m from any wall.

Generic proof: `LINDALE` (packages/synthetic-drawings) — an upper loggia with
one return and a free end whose plan draws the balustrade turning — turns
(`2 runs, 1 turn`, facade-graph TURNS_AT); its control, the same house
without the line, does not (`1 run, 0 turns`). Fixtures 1–3 (front only,
front + one side return, U-shape) hold the geometry.

## TERRACE

A first-class object (schema 1.5.0 `terraces`: polygon, level, top offset,
thickness, surface, edge, host walls, lineage through the feature graph and
the ledger). The analyzer reads the recess floor and the platform the plan
outlines beyond the mouth (`readTerraceExtension`: the first thin outline
spanning the facade, the plan between drawn on), and `closeTerraces` puts
floor edges on the returns and platform sides on the building's corners.
Auto v3: front x 0.645..11.416 × z 0..0.991; rear an 8-gon over the loggia
floor and a 7.90 × 2.11 m platform; both PAVED with a plinth, on the floor
datum (residual 0), no gap to their facades. Landscaping beyond the outline
is not modelled. Web and Android draw them (TERRACE_SURFACE); fixture 5
(terrace to a large glazed opening) holds the joint.

## FACADE ASSEMBLIES

`buildFacadeGraph`: nodes with start, end and termination; edges
CONTINUES_TO, TERMINATES_AT, TURNS_AT and MEETS_HOST with the measured gap
(17 nodes, 17 edges on Auto v3, 0 gaps). The front band runs 3.31..12.05 as
balcony slab + garage fascia with one top line (3.06) and one soffit (2.32;
truth 2.28); every return continues into its verge. Truth-v2 evaluation
(`npm run evaluate:v2`): BALCONY 0 → 1 MATCH, FACADE_MEMBER 0 → 1 MATCH, no
family lost a match.

## OPENINGS

Unchanged in this stage and source-traceable as in 03X: every facade opening
carries its callout, family and provenance; an opening whose family is not
read stays UNKNOWN rather than guessed. The source-view residuals disagree on
seven sill and head readings where glazing stands behind a balustrade or
under a balcony — identical on Auto v2 and Auto v3 (OPENINGS PARTIAL).

## SEMANTIC MATERIAL SYSTEM

`packages/mobile-scene/src/semantics.ts`: 21 semantic groups — WALL_MAIN,
WALL_SECONDARY, WALL_INTERIOR, WALL_CLADDING, ROOF_MAIN, FLAT_ROOF,
ROOF_TRIM (verge / fascia), WINDOW_GLASS, WINDOW_FRAME, DOOR, GARAGE_DOOR,
SLAB, BALCONY_SLAB, RAILING, FACADE_FRAME, TERRACE_SURFACE, CHIMNEY,
ROOFLIGHT, STAIR, ROOM, OTHER — derived from the compiled part first and the
model's facts second: an exterior wall outside every ring is a frame member;
a ring wall in a non-dominant finish is the secondary body; a slab, trim or
member built in the secondary body's finish reads as that body (the garage,
the portal band over its door and the balcony band continuing it are one
anthracite); a timber finish region is cladding. One palette
(`architectural-v1`) whose every adjacent pair differs by ≥ 0.06 relative
luminance, carried in every bundle, mirrored by the web adapter and the
Kotlin fallback, held equal by tests (`tests/architecture/styling-parity.test.ts`
compares web and phone mesh for mesh on every sealed candidate).

## SOURCE-DERIVED COLOR HINTS

The chain of §13: registered facade → region mask (the body's own open face,
clear of openings, recesses and nearer bodies; each return's outer face; each
recessed wall column by column) → exclusions (vegetation, sky and glazing) →
brightness relative to the render's own white point → robust median → tone
family (LIGHT ≥ 0.8, DARK < 0.45) → model material → palette rung. No colour
from a render reaches the screen. Auto v3: main body LIGHT (94 % of white),
garage DARK (34 %), the garage's return DARK, every gable-frame return LIGHT;
front recess timber 0.65..3.19 then anthracite 3.19..7.90 (truth 3.25), rear
recess timber. A hint moves a group only to another rung of the palette and
never into a neighbour's luminance (a dark main body would merge with its
roof and is refused). Stress: a hundred board stripes read as at most two
finish runs; a half-timber, half-dark wall as two, split within 0.1 m.

## ARCHITECTURAL PRESENTATION STYLE

Construction and Clay stay; Architectural is added on web and Android. Web:
one colour per group, a thin 0.35-opacity feature edge on structural groups
only, glazing at 0.35. Android: the same palette from the bundle, glass on
the translucent path at 0.35, subtle SSAO at half the diagnostic styles'
intensity, no outlines, no per-object colours. The web toolbar now wraps
instead of pushing the style selector off-screen (browser test at 1400 px).

## GEOMETRIC CLEANLINESS METRICS

`npm run audit:exterior` → `stage-reports/artifacts/analyzer-v2/marcowki-exterior-closure-evaluation.json`
(exterior findings):

| Metric | Auto v2 | Auto v3 |
|---|---|---|
| intersections | 13 (4.30 m³) | 0 |
| exposed gaps | 7 | 0 |
| duplicate coplanar faces | 21 (29.50 m²) | 0 |
| sliver triangles | 0 | 0 |
| non-manifold solids | 0 | 0 |
| disconnected members | 0 | 0 |
| railing free ends | 2 (max 0.94 m) | 0 (max 0.025 m) |
| terrace–datum residual | 0 | 0 |
| interior findings | 43 | 42 |

Per category (no aggregate score): MASSING, ROOF, ROOF_EDGE_CLOSURE,
WALL_SLAB_JOINTS, GARAGE_JOIN, RECESS_RETURNS, BALCONY, RAILING, TERRACE,
FACADE_ASSEMBLY, CHIMNEYS, ROOFLIGHTS **PASS**; OPENINGS and
MATERIAL_READABILITY **PARTIAL**; none FAIL.

## SOURCE-VIEW EXTERIOR AUDIT

The verifier now projects the exterior assemblies into every registered
elevation and compares each model edge with the nearest tone edge within
reach (a model edge with none is "not observed", never agreement):

| Measured | Auto v3 within / measured |
|---|---|
| roof outline | 14 / 14 |
| balcony and portal fascia top | 9 / 9 |
| balcony and portal fascia soffit | 9 / 9 |
| verge board underside | 8 / 8 |
| recess-return free face | 6 / 8 (two faces 9 cm from the nearest image edge) |
| ground-storey silhouette width | 3 / 4 (the west view 0.31 m narrow) |
| opening sill / head | 3 / 7, 2 / 7 (as Auto v2) |

Terraces are not measured on the elevations (they lie under the plinth
band); the perspectives are registered for depth order only.

## AUTO V2 VS NEW CANDIDATE

Screenshots (`npm run review:shots`), front, rear, left, right, front ¾,
rear ¾, top, roof off, each candidate alone and side by side:
`stage-reports/artifacts/exterior-closure/review/` (Architectural) and
`review-construction/` (Construction); `index.html` in each is a contact
sheet. What changed on screen: the verge is one clean white chevron per
gable instead of bars through the roof; the front band runs unbroken from
the free balcony end over the garage door; the railing turns back to the
wall; the terraces lie on the ground at both recesses, the rear with its
platform; the garage is a dark box distinct from the white house; the front
recess reads timber then anthracite; the returns do not step between
storeys.

## REFERENCE / BENCHMARK ISOLATION

`npm run reconstruct:no-reference` and `npm run reconstruct:no-benchmark`
pass (the reference package, the truth set and the research folder
physically absent). The truth set is read only by `evaluate:v2` and
`audit:exterior`, after sealing. The architecture tests hold the analyzer
free of the reference, the truth and the project's name; the LINDALE and
Kelsall fixtures and the Dunmore terrace prove the new rules on houses that
exist only in `packages/synthetic-drawings`. The two earlier candidates were
restated under schema 1.5.0 by `npm run candidates:reseal`, which proves
byte-identity of the building before recording new hashes
(`packages/candidates/src/reseal-log.json`, tested).

## ANDROID SIGNING / VERSIONING

One stable preview identity: `apps/android/keystore/preview.keystore`,
committed on purpose as a public preview key (not a production secret; its
README says what it does and does not grant), overridable by
`BUILDPLAN_PREVIEW_*` secrets without a workflow change. `versionCode` is
derived and only rises: `1000 + GITHUB_RUN_NUMBER` in CI (`versionName
0.<run>.0-preview`), the commit count locally. `applicationId` stays
`com.buildplan.preview`. CI verifies the signer
(`apps/android/tools/verify-preview-signature.sh`) and writes `VERSION.txt`
into the artifact. Install and update: `docs/ANDROID_MODEL_PREVIEW.md` —
one uninstall of any earlier preview (signed with a throwaway CI key), then
every later CI APK installs over the last.

## WEB

BuildWorld offers Reference, Demo, Auto, Auto v2 and Auto v3; styles
Construction, Clay and Architectural. Browser tests (31, `npm run e2e`): the
v3 candidate loads from its sealed program with terraces, a turning railing
and roof edge members, and ties to its hash in the sources panel; in
Architectural it draws a light main body, a darker secondary body, cladding,
terraces and trims, every group in one colour; the baseline still loads
beside it; the toolbar fits a 1400 px window.

## ANDROID

Five scene bundles exported (`npm run mobile:export-scenes`): reference,
Auto v3, Auto, Auto v2, demo — each with semantic groups and the palette
with its tone hints; CI re-exports them and fails on any difference. The JVM
tests (140, run locally against an Android SDK and in CI) hold every sealed
candidate — Auto v3 included — to the same bar: All, Roof off, Ground, Attic
and Cutaway each draw the building and not only its roof; every object with
geometry becomes an entity; wall coordinates survive to Kotlin; bounds
agree; hidden objects are not pickable; ids resolve against their own scene
(Auto v2 and Auto v3 share an id vocabulary by design, and still differ as
sets). They also hold the palette against the shared constant, the
tone-hinted bundle palettes, the cladding group and the subtle AO. What a
GPU does with it — orbit, model switch and the look of Architectural on a
phone — is an owner device check: no GPU or emulator runs in this
environment (see the checklist).

## CI RUN

Run **36240702093** (BuildApp CI #29) on `603eb2e`, all four jobs green:
<https://github.com/damiankrok/BuildApp/actions/runs/36240702093>

- **Core / Analyzer / Reconstruction**: typecheck, the vitest suite, the
  production web build, both Marcówki audits, `reconstruct:no-reference`,
  the analyzer-v2 gates, `reconstruct:no-benchmark`, `evaluate:v2`, and the
  new **exterior closure audit** (`audit:exterior`, which fails on any
  EXTERIOR closure error of the candidate under review); closure reports,
  analyzer-v2 artifacts and diagnostics uploaded.
- **Browser / Playwright**: the 31 browser tests.
- **Android / APK**: bundles re-exported and asserted current, the JVM tests,
  the preview APKs assembled, the signer verified, `VERSION.txt` recorded.
- **Dependency security** (advisory).

The two runs before it failed and were fixed, not re-run: #27 on the Android
ids test (Auto v2 and Auto v3 legitimately share ids, `74d4646`) and #28 on
the signer check reading the runner's newest apksigner (`603eb2e`).

Locally, before the push: 1182 vitest tests, 31 browser tests, 140 Android JVM
tests, the APKs assembled and their signer verified (and a foreign key
refused).

## APK ARTIFACT

`buildplan-model-preview-apks` (artifact 10905870947, 47.4 MB, kept until
2026-10-26) from run 36240702093: `BuildPlan-Model-Preview-arm64-v8a-debug.apk`
(install this on a phone), `-armeabi-v7a-`, `-x86_64-` and `-universal-debug.apk`,
and `VERSION.txt` — **versionCode 1029, versionName 0.29.0-preview**,
application id `com.buildplan.preview`, signed with the preview key (SHA-256
`6e48fac4…1ca0da`). Download it from the run page's *Artifacts* section.

## KNOWN LIMITATIONS

- **Finish regions not read**: the ground-storey dark side bands, the rear
  gable's dark panel and the attic's timber gable panel (MATERIAL_READABILITY
  PARTIAL). The recessed ground walls are read.
- **Opening sills and heads**: seven readings behind balustrade glass or
  under balconies disagree with the render (unchanged from Auto v2).
- **Two return faces and the west silhouette** differ from the nearest image
  edge by 9 cm and 0.31 m; recorded as measured.
- **Verge depth** is the depth of the zone the returns stand in: the truth
  leaves it unresolved (T2-UNRES-VERGE-DEPTH).
- **Terrain datum** under the terraces is not read: the plinth thickness is
  assumed for rendering.
- **Interior (for BUILDAPP-03Z)**: the stair shares 0.44 m³ with the ground
  ring wall and small volumes with three partitions and both chimney stacks;
  partitions stop 15 mm short of what they meet because interior junctions
  are not declared (35 INTERIOR gaps). Out of this stage's scope.
- **Android on a device** has not been exercised here; the JVM tests and the
  assembled APK are the evidence until the owner review.
- **Styling** is a controlled ladder: a dark body is drawn on the palette's
  darkest rung that stays apart from its neighbours (a warm mid-dark grey),
  not in the render's anthracite.

## OWNER REVIEW CHECKLIST

On the phone, with the new APK installed over the previous preview:

1. Model → *Marcówki (auto v3)*, style → **Architectural**. Front ¾: one white
   verge chevron per gable, no bars through the roof; the dark band over the
   balcony and the garage door is one band.
2. The balcony: its west end stops at the balustrade's turn and the railing
   turns back to the wall; its east end runs under the attic return to the
   garage fascia.
3. The terraces: the front recess floor; at the rear the loggia floor and the
   platform beyond it.
4. The garage reads as a darker body; the front recess as timber then dark.
5. **Roof off**, **Ground**, **Attic**, **Cutaway**: no slab edge or plate in
   a facade plane, no gap at the returns; the interior limitations above are
   visible in Cutaway and belong to 03Z.
6. Orbit a full turn; switch to *Marcówki (auto v2)* and back — the style
   stays Architectural; then Construction and Clay.
7. Settings → Apps → BuildPlan Model Preview shows versionName
   `0.29.0-preview` (versionCode 1029). If Android refuses the update, the
   installed preview was signed with an older throwaway key: uninstall it
   once and install again; later builds then update in place.

## NEXT TECHNICAL STEP

**BUILDAPP-03Z — Interior Topology + Semantic Completion**, as the
orchestrator has sequenced: declared interior junctions instead of trimmed
partitions (the 35 INTERIOR gaps), the stair against its walls and void (the
0.44 m³), guarding along the stair and the void, and the remaining finish
regions. Then BUILDAPP-04. This stage does not start either.

The brief asks for the project master plan to be updated to the
orchestrator's sequence. The repository has no master plan file, and
`AGENT_PROTOCOL.md` reserves the roadmap to the orchestrator and forbids the
agent from creating one; the sequence (03Z, then 04) is recorded in
`PROJECT_STATUS.md` under *Recommended technical next step* instead.

## VERDICT

The §28 criteria, each against its evidence:

| criterion | evidence |
|---|---|
| visibly better than Auto v2 | the side-by-side screenshots in both styles |
| no major exterior penetration, gap or duplicate face | closure audit: 0 exterior findings (Auto v2: 43) |
| balcony terminates at the building | every end WALL / CARRIES / FREE-at-the-drawn-line / MEETS; BALCONY PASS |
| railing turns and returns | 2 runs 1 turn, 0 free ends; Lindale and its control |
| terrace first-class | schema object, compiled, on web and Android; TERRACE PASS |
| coherent facade assemblies | one band, frames continuous, facade graph without gaps; FACADE_ASSEMBLY PASS |
| no stripe forest | 100-stripe tests for members and finish runs |
| styling reads without hiding defects | palette with adjacency gaps, no outlines, the audit gates the joints |
| web and Android show the candidate | browser tests; bundles and JVM tests; device check listed |
| stable signing and rising versions | committed preview key, versionCode 1000 + run, signer verified in CI |
| CI green, APK published | run 36240702093, artifact 10905870947 |
| honest PARTIAL list | OPENINGS and MATERIAL_READABILITY PARTIAL, the known limitations above |

**PASS_STAGE_BUILDAPP_03Y_EXTERIOR_CLOSURE_AND_SEMANTIC_STYLING** — with the
owner's device review of the APK as the final gate, as for 03X.
